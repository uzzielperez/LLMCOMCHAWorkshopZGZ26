# Archived deck (before rebuild)

Snapshot taken when trimming the live deck to a **cover-only** `slides.md`.

## Contents

| Path | Notes |
|------|--------|
| `slides.md` | Full workshop deck |
| `index.html` | Reveal shell + CSS |
| `slides-viz.js` | N-gram, attention, softmax, temperature |
| `deploy.sh` | Surge deploy |
| `artifacts/` | Standalone `explorer/` HTML |
| `scripts/` | e.g. `inject-takeaways.py` |

## Restore

From the repo root:

```bash
cp backup/pre-rebuild-2026-04-09/slides.md .
cp backup/pre-rebuild-2026-04-09/index.html .
cp backup/pre-rebuild-2026-04-09/slides-viz.js .
```

(Or copy the whole folder over your project if you prefer a full restore.)
